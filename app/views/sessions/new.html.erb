<h2 class="ui center aligned large header">
  Customer Service 

<h4 class="ui center aligned medium icon header">
  <i class="circular orange keyboard alternate icon"></i>
  Log in to continue
</h4>


<%-# favicon with some text  -%>


<div class="ui placeholder segment">
  <div class="ui two column very relaxed stackable grid">
    <div class="column">
   <%= form_for(:session, html: {class: "ui form", role: "form"}, url: login_path) do |f| %>

        <div class="field">
       <%= f.label :username, "Username" %>

          <div class="ui left icon input">
        <%= f.text_field :username, placeholder: "Username" %>

            <i class="user icon"></i>
          </div>
        </div>
        <div class="field">
        <%= f.label :password, "Password" %>
          <div class="ui left icon input">
        <%= f.password_field :password, placeholder: "Password" %>
            <i class="lock icon"></i>
          </div>
        </div>
       <%= f.button "Login", class: "ui orange submit button" %>
      <% end %>
    </div>
    <div class="middle aligned column">
     <%= button_to "Sign up Now", registration_path, method: :new , class:  "ui orange submit button" , icon: "signup icon"  %>

    </div>
    
  </div>

  <div class="ui vertical divider">
    Or
  </div>
</div>

<%-# log in and registration   -%>



<div class="ui placeholder segment">
    <div class="meta">
      <center>
      <h1> No Account No Problem! </h1>
      </center>
            <!-- <%= button_to "Chat as a guest", login_guest_path , method: :guest , class: "ui orange submit button" %>-->
            <%= link_to "Chat as a guest", login_guest_path, :class => 'ui orange submit button' %>
      </div>
    </div>

</div>

<%-# Option to chat as a quest if you don't have an account   -%>
